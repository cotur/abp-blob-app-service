version: '3.4'

services:
  migrations:
    build:
      context: ../../
      dockerfile: templates/service/database/Dockerfile
    depends_on:
      - sqlserver
    environment:
      - IdentityServer_DB=BlobManagement_Identity
      - BlobManagement_DB=BlobManagement_ModuleDb
      - SA_PASSWORD=yourStrong(!)Password
